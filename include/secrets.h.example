// Template for the real credentials file.
// Copy this to secrets.h in the same folder and fill in your own values.
// secrets.h is gitignored and must never be committed.
#pragma once

#define WIFI_SSID "your-network-name"
#define WIFI_PASSWORD "your-password"

// Where to fetch conditions for. These live here rather than in committed
// source because your coordinates are location data - the same reason the
// build photograph in the previous project had its GPS EXIF stripped.
#define LOCATION_LATITUDE 51.5072
#define LOCATION_LONGITUDE -0.1276
